# user_model_in_django
Customizing the user model in Django

After the above steps, we go to the project settings file and perform the following operations.
#settings.py
AUTH_USER_MODEL = "My_app.MyUser"
